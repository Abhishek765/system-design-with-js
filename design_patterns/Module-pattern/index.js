// Split up your code into smaller, reusable pieces

import { add, divide, multiply, subtract } from "./math.js";

const a = 10;
const b = 20;
console.log(`addition of ${a} & ${b} is: ${add(a, b)}`);
console.log(`subtraction of ${a} & ${b} is: ${subtract(a, b)}`);
console.log(`multiplication of ${a} & ${b} is: ${multiply(a, b)}`);
console.log(`division of ${a} & ${b} is: ${divide(a, b)}`);
